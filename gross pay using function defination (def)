def computepay(h, r):
    if h<=40:
        return h*r
    else:
        return ((40*r)+((h-40)*1.5*r))

hrs = input("Enter Hours:")
rph = input("rate per hour:")
h=float(hrs)
r=float(rph)
p = computepay(h,r)
print("Pay", p)